import React from 'react';
import Icons from '../OtherComponents/Icons'

const SliderButton = (props) => {
    return (
        <li className={props.className}>
            <Icons className={'dot-circle'}/>
            </li>

    )
}

export default SliderButton